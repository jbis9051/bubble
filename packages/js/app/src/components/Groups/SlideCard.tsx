import React from 'react';
import { View } from 'react-native';
import SlideCardTemplate from '../SlideCardTemplate';

const SlideCard = () => (
    <SlideCardTemplate style={{marginTop: 480}}>
        <View style={{ height: 500 }}>
        </View>
    </SlideCardTemplate>
);

export default SlideCard;